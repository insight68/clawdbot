# Marketing 页面重新设计方案

**日期**: 2025-02-03
**设计师**: Claude
**状态**: 设计完成，待实现

---

## 1. 概述

### 1.1 目标

重新设计 Marketing 路由页面，实现：

1. **展示后端 OpenClaw Skills**：筛选并展示 Marketing 相关的技能
2. **Skills 到界面的映射**：通过映射表实现分类、样式和交互的配置化管理
3. **混合布局**：顶部功能面板 + 底部聊天区域的交互模式
4. **点击交互**：点击 Skill 卡片后，将预设提示词注入到聊天区域

### 1.2 设计原则

- **配置驱动**：通过映射表管理 Skills 展示，避免硬编码
- **视觉层次**：大卡片（主要功能）vs 小卡片（具体 Skills）
- **交互流畅**：快速响应 + 流畅动画
- **主题适配**：深色/浅色主题完美支持

---

## 2. 整体架构

### 2.1 页面布局

```
┌─────────────────────────────────────────────────────────────┐
│  顶部功能面板 (30-40% 高度，可折叠)                           │
│  ┌───────────────────────────────────────────────────────┐  │
│  │ 📢 营销助手                              [折叠] [更多] │  │
│  │ 营销活动策划、内容生成和效果分析                         │  │
│  ├───────────────────────────────────────────────────────┤  │
│  │ [快捷操作区域] - 4个大卡片                             │  │
│  │  📢创建活动  ✍️生成内容  📊分析数据  🎯优化策略       │  │
│  ├───────────────────────────────────────────────────────┤  │
│  │ [Skills 网格区域] - Marketing 相关 Skills             │  │
│  │  📧邮件营销  📱社交媒体  📝广告文案  👥受众分析       │  │
│  └───────────────────────────────────────────────────────┘  │
├─────────────────────────────────────────────────────────────┤
│  底部聊天区域 (60-70% 高度)                                 │
│  ┌───────────────────────────────────────────────────────┐  │
│  │  聊天消息区域（集成现有 chat 组件）                     │  │
│  │  [输入框] [发送]                                        │  │
│  └───────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

### 2.2 组件层次

```
MarketingView (openclaw-view-marketing)
├── FunctionPanel
│   ├── PanelHeader
│   │   ├── Title
│   │   ├── Subtitle
│   │   └── CollapseButton
│   ├── QuickActions (Featured Skills - Large Cards)
│   │   └── SkillCard[] (Primary variant)
│   └── SkillsGrid (All Marketing Skills)
│       └── SkillCard[] (Secondary/Accent/Subtle variants)
└── ChatArea
    └── [集成现有 chat 组件]
```

---

## 3. Skills 映射表设计

### 3.1 数据结构

**文件位置**: `ui/src/config/skill-mappings.ts`

```typescript
// 技能分类
export type SkillCategory =
  | "campaign" // 营销活动策划
  | "content" // 内容生成
  | "analyze" // 数据分析
  | "optimize" // 策略优化
  | "automation" // 自动化工具
  | "other"; // 其他

// 视觉样式配置
export type SkillVisualStyle = {
  variant: "primary" | "secondary" | "accent" | "subtle";
  icon: string; // emoji 或图标名
  size: "large" | "medium" | "small";
  featured: boolean; // 是否显示在快捷操作区
};

// 点击交互配置
export type SkillInteraction = {
  type: "prompt" | "tool" | "link" | "modal";
  prompt?: string; // 预设提示词
  toolParams?: Record<string, unknown>;
  link?: string;
  modal?: string;
};

// 单个映射条目
export type SkillMappingEntry = {
  skillKeyPattern: string; // 支持通配符 * 和前缀匹配
  displayName?: string;
  description?: string;
  category: SkillCategory;
  visual: SkillVisualStyle;
  interaction: SkillInteraction;
  priority: number; // 排序权重
};
```

### 3.2 映表示例

```typescript
export const MARKETING_SKILL_MAPPINGS: SkillMappingEntry[] = [
  // 快捷操作（Featured Skills）
  {
    skillKeyPattern: "marketing-campaign",
    displayName: "创建营销活动",
    category: "campaign",
    visual: {
      variant: "primary",
      icon: "megaphone",
      size: "large",
      featured: true,
    },
    interaction: {
      type: "prompt",
      prompt: "请帮我创建一个营销活动策划，包括目标受众、渠道策略、预算分配和KPI指标。",
    },
    priority: 100,
  },
  // ... 更多映射条目
];
```

### 3.3 匹配函数

```typescript
// 通配符匹配
export function matchSkillPattern(skillKey: string, pattern: string): boolean;

// 查找映射配置
export function findSkillMapping(skillKey: string): SkillMappingEntry | undefined;

// 筛选 Marketing Skills
export function filterMarketingSkills(allSkills): Array<{ skill; mapping }>;

// 按分类组织
export function groupSkillsByCategory(skillsWithMapping): Record<SkillCategory, Array>;
```

---

## 4. 视觉样式设计

### 4.1 卡片变体

| 变体      | 用途                | 尺寸           | 视觉特点                       |
| --------- | ------------------- | -------------- | ------------------------------ |
| Primary   | 快捷操作区大卡片    | Large (140px)  | 渐变背景、发光阴影、大图标     |
| Secondary | Skills 网格普通卡片 | Medium (100px) | 简洁边框、左侧彩条、hover 位移 |
| Accent    | 数据分析类          | Medium         | Orange 色调、温暖感            |
| Subtle    | 自动化工具类        | Small (80px)   | 虚线边框、低调展示             |

### 4.2 状态管理

- **默认态**: 正常展示
- **Hover 态**: 位移 + 阴影 + 边框高亮
- **选中态**: 蓝色边框 + accent 背景
- **加载态**: 透明度降低 + 闪烁动画
- **禁用态**: 灰度显示 + 降低透明度

### 4.3 动画参数

- **过渡时间**: 150-200ms
- **缓动函数**: var(--ease-out)
- **Hover 位移**: translateY(-4px) 或 translateX(4px)
- **阴影增强**: 从 2px 到 12px

---

## 5. 交互逻辑

### 5.1 数据流

```
┌─────────────────────────────────────────────────────────────┐
│  后端 API                                                    │
│  GET /api/skills → 返回所有 Skills                          │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│  MarketingView 组件                                         │
│  ┌───────────────────────────────────────────────────────┐  │
│  │ 1. 接收所有 Skills                                    │  │
│  │ 2. filterMarketingSkills() - 筛选 Marketing 相关      │  │
│  │ 3. 分离 Featured 和普通 Skills                        │  │
│  │ 4. 按 category 分组普通 Skills                        │  │
│  └───────────────────────────────────────────────────────┘  │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│  SkillCard 渲染                                             │
│  - 根据 mapping.visual 配置样式                            │
│  - 根据 mapping.interaction 配置点击行为                   │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│  用户点击卡片                                               │
│  - type="prompt": 将 prompt 注入聊天输入框                  │
│  - type="tool": 调用工具 API                                │
│  - type="link": 跳转外部链接                                │
│  - type="modal": 打开模态框                                 │
└─────────────────────────────────────────────────────────────┘
```

### 5.2 点击交互实现

```typescript
private handleSkillClick(skill: any, mapping: SkillMappingEntry) {
  const { type, prompt, link, toolParams } = mapping.interaction;

  switch (type) {
    case "prompt":
      // 将提示词注入到聊天输入框
      this.dispatchEvent(new CustomEvent('inject-prompt', {
        detail: { prompt },
        bubbles: true,
        composed: true
      }));
      break;

    case "tool":
      // 调用工具 API
      this.dispatchEvent(new CustomEvent('invoke-tool', {
        detail: { toolName: skill.skillKey, params: toolParams },
        bubbles: true,
        composed: true
      }));
      break;

    case "link":
      // 打开外部链接
      window.open(link, '_blank');
      break;

    case "modal":
      // 打开模态框（TODO）
      break;
  }

  // 视觉反馈：设置选中状态
  this.selectedSkillKey = skill.skillKey;
}
```

### 5.3 聊天集成

```typescript
// 在 MarketingView 中监听提示词注入事件
// 将提示词传递给聊天组件
private handlePromptInjection(event: CustomEvent) {
  const { prompt } = event.detail;
  // TODO: 传递给聊天组件的输入框
  console.log('Inject prompt:', prompt);
}
```

---

## 6. 文件结构

```
ui/src/
├── config/
│   └── skill-mappings.ts        # Skills 映射配置
├── styles/
│   └── marketing.css            # Marketing 页面专用样式
├── ui/
│   ├── views/
│   │   └── marketing.ts         # Marketing 视图组件（更新）
│   └── types.ts                 # 类型定义（可能需要扩展）
```

---

## 7. 实现清单

- [ ] 创建 `ui/src/config/skill-mappings.ts`
- [ ] 创建 `ui/src/styles/marketing.css`
- [ ] 更新 `ui/src/ui/views/marketing.ts`
- [ ] 实现 Skills 筛选和映射逻辑
- [ ] 实现 SkillCard 组件（支持多种变体）
- [ ] 实现点击交互（提示词注入）
- [ ] 实现面板折叠功能
- [ ] 集成聊天组件
- [ ] 深色/浅色主题适配
- [ ] 响应式布局适配
- [ ] 运行 `pnpm check` 验证
- [ ] 本地测试验证

---

## 8. 后续优化

- [ ] 添加搜索/过滤功能
- [ ] 支持自定义 Skills 分类
- [ ] 添加使用统计（点击次数、常用功能）
- [ ] 支持拖拽排序
- [ ] 添加快捷键支持
- [ ] 实现 modal 类型交互
- [ ] 添加动画性能优化
