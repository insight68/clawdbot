'use client';

export default function SessionsPage() {
  return (
    <div style={{ padding: '2rem' }}>
      <h1>会话</h1>
      <p style={{ color: 'var(--text-secondary)' }}>
        检查活动会话并调整会话默认值
      </p>
    </div>
  );
}
